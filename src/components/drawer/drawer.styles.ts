import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    color: '#FFFFFF',
    fontWeight: '900',
    fontSize: 20,
    textAlign: 'center',
    paddingVertical: 40,
  },
});
